pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Compile and build your application...'
            }
        }
        stage('Test') {
            steps {
                echo 'Run tests and quality checks...'
            }
        }
        stage('Deploy to Staging') {
            steps {
                echo 'Deploy to a staging environment...'
            }
        }
        stage('Deploy to Production') {
            steps {
                echo 'To Deploy to production with the chosen strategy...'
            }
        }
    }
}
